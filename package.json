{
  "name": "soulbuilder-monorepo",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "pnpm --filter @soulbuilder/app dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
